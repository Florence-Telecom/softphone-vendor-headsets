import { Observable } from 'rxjs';
import { VendorImplementation, ImplementationConfig, ImplementationConnectionOptions } from './vendor-implementations/vendor-implementation';
import { CallInfo } from '../types/call-info';
import { HeadsetIntegrationStatus } from '../types/emitted-headset-events';
import { ConsumedHeadsetEvents, DeviceConnectionStatus } from '../types/consumed-headset-events';
import { UpdateReasons } from '../types/headset-states';
export default class HeadsetService {
    private static instance;
    plantronics: VendorImplementation;
    jabraNative: VendorImplementation;
    jabra: VendorImplementation;
    sennheiser: VendorImplementation;
    yealink: VendorImplementation;
    vbet: VendorImplementation;
    cyberAcoustics: VendorImplementation;
    selectedImplementation: VendorImplementation;
    headsetEvents$: Observable<ConsumedHeadsetEvents>;
    private headsetConversationStates;
    private _headsetEvents$;
    private logger;
    private implementationTransitionGeneration;
    private constructor();
    static getInstance(config: ImplementationConfig): HeadsetService;
    get implementations(): VendorImplementation[];
    private isDifferentState;
    private updateHeadsetState;
    deviceIsSupported(params: {
        micLabel: string;
    }): boolean;
    activeMicChange(newMicLabel: string, changeReason?: UpdateReasons): void;
    changeImplementation(implementation: VendorImplementation | null, deviceLabel: string, options?: ImplementationConnectionOptions): Promise<void>;
    incomingCall(callInfo: CallInfo, hasOtherActiveCalls?: boolean): Promise<any>;
    outgoingCall(callInfo: CallInfo): Promise<any>;
    answerCall(conversationId: string, autoAnswer?: boolean): Promise<any>;
    rejectCall(conversationId: string, expectExistingConversation?: boolean): Promise<any>;
    setMute(value: boolean): Promise<any>;
    setHold(conversationId: string, value: boolean): Promise<any>;
    endCall(conversationId: string, hasOtherActiveCalls?: boolean): Promise<any>;
    endAllCalls(): Promise<any>;
    retryConnection(micLabel: string, options?: ImplementationConnectionOptions): Promise<void>;
    integrationStatus(): HeadsetIntegrationStatus | undefined;
    connectionStatus(): DeviceConnectionStatus;
    resetHeadsetStateForCall(conversationId: string): Promise<any>;
    private getConnectedImpl;
    private subscribeToHeadsetEvents;
    private clearSelectedImplementation;
    private setRemoveTimer;
    private handleDeviceAnsweredCall;
    private handleDeviceRejectedCall;
    private handleDeviceEndedCall;
    private handleDeviceMuteStatusChanged;
    private handleDeviceHoldStatusChanged;
    private handleDeviceConnectionStatusChanged;
    private publishConnectionStatus;
    private handleWebHidPermissionRequested;
    private handleIntegrationStatusChanged;
    private handleDeviceLogs;
    private isEventFromSelectedImplementation;
}
