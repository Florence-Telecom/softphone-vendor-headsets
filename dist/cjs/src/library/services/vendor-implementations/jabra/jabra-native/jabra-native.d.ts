import { VendorImplementation, ImplementationConfig } from '../../vendor-implementation';
import DeviceInfo from '../../../../types/device-info';
import { CallInfo } from '../../../../types/call-info';
import { JabraNativeHeadsetState } from './types';
export default class JabraNativeService extends VendorImplementation {
    private static instance;
    isActive: boolean;
    devices: Map<string, DeviceInfo>;
    activeDeviceId: string;
    headsetState: JabraNativeHeadsetState;
    ignoreNextOffhookEvent: boolean;
    cefSupportsJabra: boolean;
    pendingConversationId: string;
    activeConversationId: string;
    pendingConversationIsOutbound: boolean;
    private constructor();
    private isHostedContextInitialized;
    private waitForHostedContext;
    private setupNativeHandlers;
    static getInstance(config: ImplementationConfig): JabraNativeService;
    get deviceInfo(): DeviceInfo;
    get deviceName(): string;
    get isDeviceAttached(): boolean;
    private isHeadsetEvent;
    private isDeviceEvent;
    private handleCefEvent;
    private handleJabraDeviceAttached;
    private handleJabraEvent;
    private _handleOffhookEvent;
    private _handleMuteEvent;
    private _handleHoldEvent;
    private _getHeadsetIntoVanillaState;
    private _sendCmd;
    private _setRinging;
    isSupported(): boolean;
    deviceLabelMatchesVendor(label: string): boolean;
    setMute(value: boolean): Promise<void>;
    setHold(conversationId: string, value: boolean): Promise<void>;
    incomingCall(callInfo: CallInfo): Promise<void>;
    answerCall(conversationId: string, autoAnswer?: boolean): Promise<void>;
    rejectCall(): Promise<void>;
    outgoingCall(callInfo: CallInfo): Promise<void>;
    endCall(conversationId: string, hasOtherActiveCalls: boolean): Promise<void>;
    endAllCalls(): Promise<void>;
    updateDevices(): Promise<void>;
    private _processEvent;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
}
